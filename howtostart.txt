1. Backend:

for backend first copy the .env.txt values and make a file .env and paste all the values

2. 🐳 Run the Entire Application Using Docker (Recommended)

This project is fully Dockerized. You can run both backend and frontend with a single command.

### Steps:
1. Make sure **Docker Desktop** is installed and running.
2. In the project root folder (where `docker-compose.yml` is located), run:
```
   docker compose up --build
```
3. Wait for the build to complete. Then open:
   - Frontend → http://localhost:3000
   - Backend → http://localhost:5001

4. To stop the containers:
   docker compose down

---

✅ That’s it! The application should now be up and running.

just clone the project and run;
   docker compose up --build

   the server will start and visit the provided urls or open with docker desktop


-Production Link: https://crm-job-assesment.onrender.com